
# this file holds other application-wide constants or configurations
# that are NOT API keys (which go in .env) and NOT Streamlit UI themes
# (which go in .streamlit/config.toml).

DEFAULT_MAX_QUESTIONS = 20
DEFAULT_LLM_MODEL = "gpt-3.5-turbo"
LLM_TEMPERATURE = 0.7 #to control randomness of llm outputs